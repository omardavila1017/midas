// ProposalCreator.tsx — retired. The Propuestas UI is now managed by
// ScenarioWorkbench and the new inline form panel. This stub keeps the
// TypeScript build green while we finish removing the file entirely.
export default function ProposalCreator(): null {
  return null;
}
