// ProposalTimeline.tsx — retired. Timeline rendering is now part of
// ScenarioWorkbench. Stub kept so legacy re-exports continue to compile.
export default function ProposalTimeline(): null {
  return null;
}
