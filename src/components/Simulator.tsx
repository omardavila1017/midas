// Simulator.tsx — retired. Functionality now lives inside ScenarioWorkbench.
// This stub exists only so the TypeScript build does not trip over the old
// file while we finish the v4 refactor. It can be deleted later.
export default function Simulator(): null {
  return null;
}
