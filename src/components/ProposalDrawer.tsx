// ProposalDrawer.tsx — retired. Proposal editor now lives inside
// ScenarioWorkbench's inline form. Stub kept to avoid orphan breakage.
export default function ProposalDrawer(): null {
  return null;
}
