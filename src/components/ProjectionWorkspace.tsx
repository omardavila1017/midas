// ProjectionWorkspace.tsx — retired. Proyección ahora vive repartido entre
// Forecast y ScenarioWorkbench. Mantengo este stub para que el build de
// TypeScript no falle sobre código huérfano mientras terminamos el refactor.
export default function ProjectionWorkspace(): null {
  return null;
}
